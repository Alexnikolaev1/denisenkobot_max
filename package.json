{
  "name": "denisenko-bot",
  "version": "2.0.0",
  "description": "Production-ready Telegram-бот для фотостудии Татьяны Денисенко",
  "main": "src/bot.js",
  "scripts": {
    "start": "node max-local.js",
    "dev": "node max-local.js",
    "set-subscriptions": "node scripts/set-subscriptions-max.js",
    "check": "node -e \"process.env.BOT_TOKEN = process.env.BOT_TOKEN || 'test_token'; require('./src/max/bot')(); console.log('max bot modules: ok')\""
  },
  "dependencies": {
    "@maxhub/max-bot-api": "^0.2.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.x"
  }
}
